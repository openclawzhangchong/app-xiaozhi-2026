name: xiaozhi_client_flutter
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  # 保留 Dart 3.13（Flutter 3.47.2）：业务包（riverpod 3.4.3 / go_router 18 / record 7 等）
  # 要求 Dart >=3.12；代码生成栈降至 freezed 2.4.7 自洽档（analyzer 6.x）。
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.9

  # 状态管理
  flutter_riverpod: ^3.4.3

  # 网络请求
  dio: ^5.11.1
  retrofit: ^4.10.0
  http: ^1.6.0
  web_socket_channel: ^3.0.3

  # 本地存储
  shared_preferences: ^2.5.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.6

  # 路由导航
  go_router: ^18.0.1

  # JSON 序列化
  json_annotation: 4.8.1
  freezed_annotation: 2.4.1

  # UI 组件
  cached_network_image: ^4.0.0
  flutter_svg: ^2.3.0
  shimmer: ^4.0.0

  # 多媒体
  image_picker: ^1.2.3
  permission_handler: ^13.0.2

  # 工具类
  crypto: ^3.0.7
  logger: ^2.7.0
  fluttertoast: ^10.0.0
  intl: ^0.20.3
  uuid: ^4.6.0
  device_info_plus: ^13.2.0
  record: ^7.1.1
  opus_dart: ^3.0.1
  audio_session: ^0.2.4
  flutter_pcm_sound: ^3.3.3
  logging: ^1.3.0
  vibration: ^3.2.1
  opus_flutter: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 代码生成（精确钉版，避免上浮到破坏 Dart 3.13 的版本）
  # 关键：freezed 必须钉在 2.4.7 —— 2.5.x 会在工厂构造函数参数生成 `required final`，
  #       该写法在 Dart 3.13.2 下非法（"Can't have modifier 'final' here"）。
  #       上游 xiaozhi-client-flutter 之所以能用 ^2.4.7 成功，是因为它跑 Dart 3.8（接受该写法），
  #       而本仓库业务包（riverpod 3.4.3 等）要求 Dart >=3.12，无法降级 Dart，故钉 freezed=2.4.7。
  # 注：json_serializable 必须钉在 6.7.1（6.8.0 要求 json_annotation >=4.9.0，与本仓库 4.8.1 冲突）。
  build_runner: 2.4.13
  json_serializable: 6.7.1
  freezed: 2.4.7
  # 注：hive_generator / retrofit_generator 已被移除——本仓库未使用 @HiveType / @RestApi 代码生成。
  # 注：mockito 已移除——代码库无任何 @GenerateMocks / import 'package:mockito' 引用，且 mockito 5.8.1
  #     要求 analyzer >=13.3，与 freezed 2.4.7 自洽栈（analyzer 6.x）互斥。

  # 代码质量
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here, each
  # entry in this list should have a "family" key with the font family name,
  # and a "fonts" key with a list giving the asset and other descriptors for
  # the font. For example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
